name: x86_64-npt-gcc-13-kernel-4.19.56-shared-vdso-elf

on:
  workflow_dispatch:
  repository_dispatch:
    types: [test-update]
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - master


jobs:

  call_build:
    uses: ./.github/workflows/z_build_workflow.yml
    with:
      toolchain: toolchain-x86_64-gcc-13
      gcc_prefix: x86_64-openadk-linux-uclibc-
      kernel_vers: 4.19.56
      kernel_dir: v4.x
      kernel_arch: x86_64
      config_file: config.x86_64_shared_vdso
      qemu_kernel: kernel-x86_64.img
      qemu_cmd: qemu-system-x86_64 -machine accel=kvm:tcg -m 1G -smp 4 -append "console=ttyS0" -kernel kernel.img  -initrd rootfs.img.xz -nographic


  call_images:
    needs: call_build
    uses: ./.github/workflows/z_images_workflow.yml
    secrets: inherit


  call_qemu:
    needs: call_images
    uses: ./.github/workflows/z_qemu_test_run.yml
    secrets: inherit

  call_badge_failed:
    if: failure()
    needs: [call_build, call_images]
    uses: ./.github/workflows/z_badge_failed_workflow.yml
    with:
      build_result: ${{ needs.call_build.result }}
      images_result: ${{ needs.call_images.result }}
    secrets: inherit
