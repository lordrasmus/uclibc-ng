name: i686-npt-gcc-13-kernel-6.18.33-shared-elf

on:
  workflow_dispatch:
  repository_dispatch:
    types: [test-update]
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - master


jobs:

  call_build:
    uses: ./.github/workflows/z_build_workflow.yml
    with:
      toolchain: toolchain-i686-gcc-13
      gcc_prefix: i686-openadk-linux-uclibc-
      kernel_vers: 6.18.33
      kernel_dir: v6.x
      kernel_arch: x86
      config_file: config.x86-time64
      qemu_kernel:  kernel-i386-6.18.33.img
      qemu_cmd:  qemu-system-i386 -machine accel=kvm:tcg -m 1G -smp 4 -append "console=ttyS0" -nographic  -kernel kernel.img -initrd rootfs.img

  call_images:
    needs: call_build
    uses: ./.github/workflows/z_images_workflow.yml
    secrets: inherit


  call_qemu:
    needs: call_images
    uses: ./.github/workflows/z_qemu_test_run.yml
    secrets: inherit
